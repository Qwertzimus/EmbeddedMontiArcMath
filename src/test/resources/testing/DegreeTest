package testing;

component DegreeTest {
  ports in (0° : 1° : 180°) in1,
           (0° : 1° : 180°) in2,
       out (0° : 1° : 180°) out1;
       
  implementation Math {
    out1 = in1 + in2;
  }
}
